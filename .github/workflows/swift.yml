name: iOS CI

on:
  push:
    branches: [ main, feature, bugfix ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  smoke:
    name: "Smoke"
    runs-on: ubuntu-latest
    steps:
      - name: Show context
        run: echo "workflow is parseable and running"
